# Interactive Pi Broker v0.1.0 — final acceptance evidence

Commit: `3b8e058f0cf0f03ecf9cdcaf8a3bf625ba1fe7bf` (main, after Tasks 1-6 and the README rewrite)
Worktree: fresh detached checkout at `../.wt/pi-broker-sc-07-final`, `npm ci` only, no symlink or external fixture.
Node: v22.23.1. Pi: 0.84.1 (`/opt/homebrew/bin/pi`).

## Commands run, twice, from this fresh detached checkout

| Command | Run 1 | Run 2 |
|---|---|---|
| `npm ci` | exit 0 | (provisioning step, once) |
| `npm test` | 8 tests, 8 pass, 0 fail | 8 tests, 8 pass, 0 fail |
| `npm run poc` | exit 0, verdict `PASS` | exit 0, verdict `PASS` |
| `npm run poc:hosts` | exit 0, verdict `PASS` | exit 0, verdict `PASS` |

### `npm run poc` assertions (run 1, all `true`)

```json
{
  "broker_prompt_enters_as_extension_user_input": true,
  "graceful_remote_shutdown": true,
  "host_neutral_cli_returns_target_response": true,
  "human_and_broker_share_same_interactive_session": true,
  "permission_layer_denies_delete": true,
  "permission_layer_denies_worktree_escape": true,
  "process_table_confirms_no_headless_flags": true,
  "remote_interrupt_returns_session_to_idle": true,
  "remote_prompt_response_visible_in_real_tui": true,
  "targeted_routing_reaches_second_session": true,
  "two_interactive_sessions_registered": true
}
```
`credentials: false`, `external_network: false`, `pi_command_shape: "interactive; no -p, --print, --mode json, or --mode rpc"`. Identical on run 2.

### `npm run poc:hosts` (run 1)

```json
{
  "assertions": {
    "claude_code_connected": true,
    "codex_enabled": true,
    "isolated_configuration_removed": true,
    "opencode_connected": true
  },
  "global_configuration_modified": false,
  "host_versions": {
    "claude": "2.1.227 (Claude Code)",
    "codex": "codex-cli 0.147.0",
    "opencode": "1.18.5"
  },
  "model_calls": false,
  "verdict": "PASS"
}
```
Identical on run 2.

### Standalone Pi headless modes preserved

```
pi --help | rg -- '--mode <mode>|--print, -p'
  --mode <mode>                  Output mode: text (default), json, or rpc
  --print, -p                    Non-interactive mode: process prompt and exit
```

## Public-safety gauntlet

- `gitleaks detect --no-banner --source . --no-git --redact --exit-code 1` — scanned ~117.72 KB, **no leaks found**, exit 0.
- `npm audit --omit=dev` — **0 vulnerabilities**, exit 0.
- `npm pack --dry-run --json` file list: `LICENSE`, `PROOF-INDEX.md`, `README.md`, `SECURITY.md`, `bin/pi-broker.mjs`, `extensions/pi-broker-bridge.ts`, `package.json`, `src/broker.mjs`, `src/client.mjs`, `src/mcp-server.mjs`, `src/protocol.mjs` — matches `package.json`'s declared `files` allowlist exactly; no test fixtures, no scratch artifacts, no personal path.

## Whole-tree status

`git status --porcelain` — empty (clean) after all of the above.
`git status --porcelain --ignored`, excluding `node_modules/` — no `.DS_Store`, `__pycache__`, `.pyc`, or `.sock` artifacts.

## Strong Cards closure

Tasks 1-6, all `complete`, all independently verified — not from worker self-report. Notable incidents during this run, both resolved and fed back into this project's internal Strong Cards process documentation:

1. **Whole-file reformatting defect (SC-03, SC-04, SC-02).** Root cause: `pi-lens`, a globally installed Pi extension, auto-runs Biome on any edited `.js`/`.ts` file when the target repo has no committed formatter config — independent of model (two different models both reproduced it identically). Fixed per-incident via `expand -t 2` + diff-against-baseline; fixed structurally going forward via `--no-autoformat` on dispatch. Internal process documentation (`RULES.md` §9 / RULE 9.9, `JUDGE-PROTOCOL.md`) updated with the definitive cause and a requirement that model-tier escalation be justified, not assumed, before use.
2. **Documentation grounding gap (SC-05 fix round 1).** First-pass README/SECURITY.md content used a fabricated `--socket` CLI flag and omitted the `PI_BROKER_SOCKET`/`PI_BROKER_SESSION_ID` environment variables the bridge extension actually requires to connect — caught by independent verification against the merged `bin/pi-broker.mjs` and `extensions/pi-broker-bridge.ts`, not by trusting the worker's report. Corrected in one same-tier fix round.

No `INVALID_CARD`, no `BLOCKED` card, no parked findings requiring human adjudication.

## Verdict

**v0.1 ACCEPTED.** All success criteria SC-1 through SC-7 satisfied with evidence above and in `PROOF-INDEX.md`.
